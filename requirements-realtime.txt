# Additional requirements for real-time market data integration
# Add these to your existing requirements.txt

# For WebSocket support (optional - only if you want real-time streaming)
websocket-client>=1.6.0

# HTTP requests (should already be installed)
requests>=2.28.0

# Data manipulation (should already be installed)
pandas>=1.5.0
numpy>=1.21.0